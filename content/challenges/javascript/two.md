---
title: test 2
---

  ```javascript
  function validBraces(braces){
  //TODO input string
  
  const stack = []
  
  // If string length is odd return false
  if (braces.length % 2 !== 0) return false
  
  const map = new Map([
    ['(', ')'],
    ['[', ']'],
    ['{', '}'],
  ])
}
  ```